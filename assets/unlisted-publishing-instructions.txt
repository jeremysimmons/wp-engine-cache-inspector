Chrome Web Store — Unlisted Publishing

Item ID: onenpcgedbffidbkpeamobmhlnfgaeng
https://chromewebstore.google.com/detail/onenpcgedbffidbkpeamobmhlnfgaeng

Upload as a new version of this existing item. Do not click Add new item.
Store listing, screenshots, privacy, permission justifications, and visibility stay on the item and do not need to be re-entered.

1. Run `npm run package`.
2. Open the existing item in the Chrome Web Store Developer Dashboard.
3. On the Package tab, upload `wp-engine-cache-inspector-<version>.zip`.
4. Submit for review.

The zip omits `key` (Chrome Web Store rejects it). `dist/` keeps `key` so Load unpacked uses the same ID.
